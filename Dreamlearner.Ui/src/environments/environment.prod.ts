export const environment = {
  production: true,
  apiUrl: 'https://api.dreamlearner.pt'   // ← your real production URL later
  // or leave as '' if you use build-time replacement or a different strategy
};
